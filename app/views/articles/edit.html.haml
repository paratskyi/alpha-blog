%h1 Edit article

- if @article.errors.any?
  %h2 The following errors prevented  the article form being save
  %ul
    - @article.errors.full_messages.each do |message|
      %li= message

= form_with(model: @article, local: true) do |f|
  %p
    = f.label :title
    %br/
    = f.text_field :title
  %p
    = f.label :description
    %br/
    = f.text_area :description
  %p
    = f.submit

%p
  = link_to 'Cancel and return to articles listing', :articles


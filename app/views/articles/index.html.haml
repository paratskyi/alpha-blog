%h1.text-center.mt-4 Listing all articles
.flickr_pagination
  = will_paginate @articles, :container => false
= render 'article'
.flickr_pagination.mb-4
  = will_paginate @articles, :container => false
